"use client";
import {Chess} from "chess.js";import {useMemo,useState} from "react";
const piece:Record<string,string>={wk:"♔",wq:"♕",wr:"♖",wb:"♗",wn:"♘",wp:"♙",bk:"♚",bq:"♛",br:"♜",bb:"♝",bn:"♞",bp:"♟"};
type Props={fen:string;onSquare?:(square:string)=>void;onMove?:(move:string,legal:boolean)=>void;target?:string};
export function Chessboard({fen,onSquare,onMove,target}:Props){const chess=useMemo(()=>new Chess(fen),[fen]);const [selected,setSelected]=useState<string>();const [position,setPosition]=useState(chess.fen());const board=useMemo(()=>new Chess(position),[position]);const legal=selected?board.moves({square:selected,verbose:true}).map(m=>m.to):[];const rows=[8,7,6,5,4,3,2,1];const files=["a","b","c","d","e","f","g","h"];
function click(square:string){onSquare?.(square);if(!selected){if(board.get(square as never)?.color==="w")setSelected(square);return;}const move=`${selected}-${square}`;try{const result=board.move({from:selected,to:square,promotion:"q"});if(result){setPosition(board.fen());onMove?.(move,true);}else onMove?.(move,false);}catch{onMove?.(move,false)}setSelected(undefined)}
return <div className="board" role="grid" aria-label="Interactive chess board">{rows.flatMap((rank,ri)=>files.map((file,fi)=>{const square=`${file}${rank}`;const p=board.get(square as never);const color=(ri+fi)%2===0?"light":"dark";return <button role="gridcell" aria-label={square} key={square} className={`square ${color} ${selected===square?"selected":""} ${legal.includes(square)?"legal":""} ${target===square?"target":""}`} onClick={()=>click(square)}>{p&&piece[`${p.color}${p.type}`]}{(fi===0||ri===7)&&<span className="coord">{fi===0?rank:""}{ri===7?file:""}</span>}</button>}))}</div>}
