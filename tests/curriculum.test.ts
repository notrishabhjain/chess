import {describe,expect,it} from "vitest";import {beginnerLessons} from "../lib/curriculum/lessons";import {pick} from "../lib/i18n";import {defaultProgress,nextLessonId} from "../lib/progress/model";
describe("bilingual curriculum",()=>{it("contains complete English and Hindi content",()=>{for(const lesson of beginnerLessons){expect(pick(lesson.title,"en")).toBeTruthy();expect(pick(lesson.title,"hi")).toBeTruthy();expect(pick(lesson.exercise.hint,"hi")).toBeTruthy();}});it("keeps recommended lesson independent of language",()=>{expect(nextLessonId(defaultProgress,beginnerLessons.map(x=>x.id))).toBe("orientation");});});
