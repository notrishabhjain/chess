"use client";import Link from "next/link";import {beginnerLessons} from "@/lib/curriculum/lessons";import {useApp} from "@/components/layout/app-provider";
export default function Learn(){const {progress}=useApp();const l=progress.locale;return <main className="shell page"><div className="eyebrow">{l==="en"?"Level 1 curriculum":"स्तर 1 पाठ्यक्रम"}</div><h1 className="section-title" style={{marginTop:8}}>{l==="en"?"Chess fundamentals":"शतरंज की बुनियाद"}</h1><div className="grid">{beginnerLessons.map(x=>{const state=progress.lessons[x.id]?.status;return <article className="card" key={x.id}><span className="tag">{state==="completed"?(l==="en"?"Completed":"पूर्ण"):`${l==="en"?"Lesson":"पाठ"} ${x.order}`}</span><h3>{x.title[l]}</h3><p>{x.description[l]}</p><Link className="button secondary" href={`/learn/${x.id}`}>{state==="completed"?(l==="en"?"Review":"दोहराएँ"):(l==="en"?"Start lesson":"पाठ शुरू करें")}</Link></article>})}</div></main>}
